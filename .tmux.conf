# change some colors
set -g status-bg black
set -g status-fg blue
set-window-option -g automatic-rename off
# make current window name bold
setw -g window-status-current-attr bold

new -d
neww -d
join-pane -h -s 1 -p 30
